import { DomSanatizerPipe } from './dom-sanatizer.pipe';

describe('DomSanatizerPipe', () => {
  it('create an instance', () => {
    const pipe = new DomSanatizerPipe();
    expect(pipe).toBeTruthy();
  });
});
